# =============================================================================
# Drama Shorts Web 환경 변수 설정
# =============================================================================
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. 아래 가이드에 따라 값 수정
# 3. run.sh 또는 dev.sh 실행
# =============================================================================

# -----------------------------------------------------------------------------
# Django 기본 설정
# -----------------------------------------------------------------------------

# SECRET_KEY: Django 보안 키 (필수)
# - 프로덕션에서는 반드시 고유한 값 사용
# - 생성 방법: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# - 또는: openssl rand -base64 50
DJANGO_SECRET_KEY="your_django_secret_key"

# DEBUG: 디버그 모드 (True/False)
# - 로컬 개발: True (상세 에러 페이지, 디버그 툴바)
# - 프로덕션: False (보안상 필수)
DJANGO_DEBUG="your_django_debug"

# ALLOWED_HOSTS: 허용할 호스트명 (쉼표로 구분)
# - 로컬: localhost,127.0.0.1
# - 프로덕션: IP주소,도메인명
# - 예시: 13.125.xxx.xxx,drama.buuup.kr
DJANGO_ALLOWED_HOSTS="your_django_allowed_hosts"

# CSRF_TRUSTED_ORIGINS: CSRF 신뢰 출처 (쉼표로 구분, 프로토콜 포함)
# - 로컬: http://localhost:8000,http://127.0.0.1:8000
# - 프로덕션: http://IP주소,https://도메인
# - 예시: http://13.125.xxx.xxx,https://drama.buuup.kr
DJANGO_CSRF_TRUSTED_ORIGINS="your_django_csrf_trusted_origins"

# -----------------------------------------------------------------------------
# 관리자 계정 (초기 슈퍼유저)
# -----------------------------------------------------------------------------
# 컨테이너 최초 실행 시 자동 생성됨
# 이미 존재하면 무시됨

DJANGO_SUPERUSER_USERNAME="your_django_superuser_username"
DJANGO_SUPERUSER_EMAIL="your_django_superuser_email"
DJANGO_SUPERUSER_PASSWORD="your_django_superuser_password"

# -----------------------------------------------------------------------------
# AWS S3 설정 (선택사항 - 미디어 파일 저장용)
# -----------------------------------------------------------------------------
# 설정하지 않으면 로컬 파일 시스템 사용
# provision_aws.sh 실행 후 출력되는 값으로 설정

# Access Key ID: IAM 사용자 액세스 키
# - AWS Console > IAM > 사용자 > 보안 자격 증명 > 액세스 키 생성
# - 또는 provision_aws.sh 실행 시 자동 생성
AWS_ACCESS_KEY_ID=your_aws_access_key_id

# Secret Access Key: IAM 사용자 시크릿 키
# - Access Key 생성 시 한 번만 표시됨 (반드시 저장)
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# S3 Bucket Name: 미디어 저장용 버킷
# - provision_aws.sh가 생성: drama-shorts-media-{account-id}
# - 직접 생성 시: 전역적으로 고유한 이름 필요
AWS_STORAGE_BUCKET_NAME="your_aws_storage_bucket_name"

# S3 Region: 버킷 리전
# - 서울: ap-northeast-2
# - 도쿄: ap-northeast-1
# - 버지니아: us-east-1
AWS_S3_REGION_NAME="your_aws_s3_region_name"

LANGSMITH_TRACING="your_langsmith_tracing"
LANGSMITH_ENDPOINT="your_langsmith_endpoint"
LANGSMITH_API_KEY="your_langsmith_api_key"
LANGSMITH_PROJECT="your_langsmith_project"

GEMINI_API_KEY="your_gemini_api_key"
GEMINI_AI_MODEL="your_gemini_ai_model"

OPENAI_API_KEY="your_openai_api_key"
REPLICATE_API_TOKEN="your_replicate_api_token"